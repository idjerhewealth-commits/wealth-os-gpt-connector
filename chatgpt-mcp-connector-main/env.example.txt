# Copy to .env for local testing, or set these as secrets/env vars on your
# hosting platform (Railway, Render, Fly.io, etc.)

OPENAI_API_KEY=sk-...

# Shared secret required in the Authorization: Bearer header. Without this
# the server is open to anyone who finds the URL. Generate one with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
MCP_AUTH_TOKEN=

# Optional
PORT=8000
OPENAI_DEFAULT_MODEL=gpt-4o
